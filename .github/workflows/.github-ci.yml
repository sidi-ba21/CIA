name: CI

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  backend:
    name: Back-end
    uses: ./ops/github/pipelines/.github-ci.back.yml

  frontend:
    name: Front-end
    uses: ./ops/github/pipelines/.github-ci.front.yml